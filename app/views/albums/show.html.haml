%article.album
  %h1.title= @album.name
  %h2.artists= "Artist".pluralize(@album.artists.count)
  %ul.artists_list 
    - @album.artists.each do |artist|
      %li.artist_name= artist.name
  %h2.songs Songs:
  %ul.songs_list
    - @album.songs.each do |song|
      %li.song_name= song.name
