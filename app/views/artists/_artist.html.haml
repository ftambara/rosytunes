%article.artist{class: "md:w-3/4"}
  %header{class: "flex gap-8 bg-rose-700 text-white p-8 rounded-t-lg"}
    - if artist.cover
      .cover{class: "grow-0 w-40 h-40"}
        %p{class: "text-sm"} Listen on Spotify
        = link_to (image_tag artist.cover, class: "block h-36 w-36"),
          artist.external_uri
    .info{class: "w-full flex gap-8 justify-between"}
      %h1.title{class: "inline"}
        = artist.name
      .bookmark{class: "flex-none"}
        - if user
          = render "artist_bookmarks/bookmark_toggle_button", user:, artist:
  .albums{class: "w-full flex flex-wrap justify-around gap-8 p-8 rounded-b-lg bg-rose-200"}
    - artist.albums.each do |album|
      - cache album do
        = render "albums/card", album:
